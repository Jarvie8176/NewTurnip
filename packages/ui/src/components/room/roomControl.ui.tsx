import React, { Fragment } from "react";
import { ButtonUI } from "../common/ButtonUI.ui";

export const RoomControl = () => {
  return (
    <Fragment>
      <ButtonUI text={"下一个"} />
    </Fragment>
  );
};
